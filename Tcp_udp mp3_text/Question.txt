Write a TCP & UDP file transfer program, where client sends a MP3 file to a the server. While writing the program you might be needing additional header file, which you can get it in C++. Also rectify the issues with the existing program in video. Later you can try for different file format including video, audio, text, etc. Also analyse the data transmission latency using network commands.
